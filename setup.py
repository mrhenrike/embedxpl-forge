# Author: André Henrique (@mrhenrike) | União Geek — https://github.com/Uniao-Geek
# Legacy shim — actual metadata lives in pyproject.toml (PEP 517/518).
# Keep this file so that `pip install -e .` works on older pip versions.

from setuptools import setup

setup()
