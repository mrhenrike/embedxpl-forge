%% RouterXPL-Forge — managed L2/L3 switch (en-US)
flowchart TB
  CORE(("Managed switch<br/>ASIC + ARM/MIPS control plane<br/>Proprietary NOS / Cisco IOS-like / RouterOS L2"))

  MGT["Out-of-band MGMT port<br/>✓ same service stack as routers"]

  HTTP["HTTP/S switch GUI<br/>✓ exploits where HTTP-like admin"]
  SSH["SSH / reverse SSH<br/>✓ creds"]
  TN["Telnet<br/>✓ creds"]
  SNMP["SNMP bridges / STP / VLAN MIBs<br/>✓ community tests"]
  STP["Control-plane protocols<br/>STP/LACP — ◐ no dedicated pwn stack"]

  CVE["Historically: weak SNMP • default creds • HTTP RCE on smart switches<br/>✓ partial vendor coverage + generic"]

  MGT --- CORE
  CORE --- HTTP
  CORE --- SSH
  CORE --- TN
  CORE --- SNMP
  CORE --- STP
  HTTP --- CVE
