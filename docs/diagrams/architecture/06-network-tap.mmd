%% RouterXPL-Forge — network TAP / passive visibility (en-US)
flowchart TB
  CORE(("Network TAP / packet broker<br/>ASIC mirror • FPGA • timestamping<br/>Minimal control plane"))

  INLINE["Inline passive taps<br/>◐ L1/L2 — no TCP services on tap path"]
  MGT["Management HTTPS / SSH mgmt-only<br/>✓ creds when exposed"]
  API["REST / SNMP config<br/>◐ vendor-specific"]

  OUT["RouterXPL-Forge: PCAP modules + CVE lookup<br/>target_device_class tap — few dedicated exploits"]

  CORE --- INLINE
  CORE --- MGT
  CORE --- API
  MGT --- OUT
