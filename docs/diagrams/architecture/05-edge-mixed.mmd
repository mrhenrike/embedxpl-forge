%% EmbedXPL-Forge — mixed small-office edge (router + security features) (en-US)
flowchart TB
  CORE(("Mixed UTM-lite / router-fw<br/>Single box: routing + DPI-lite + guest Wi‑Fi"))

  subgraph COMBO["Merged attack surfaces"]
    R["Router stack<br/>NAT • VPN client • port fw • UPnP<br/>✓ router exploit family"]
    U["Security features<br/>URL filter • AV proxy • IPS feed<br/>◐ mgmt plane only typical"]
  end

  V1["HTTP/S admin — ✓✓ highest yield"]
  V2["SSH/Telnet — ✓"]
  V3["Site-to-site VPN — ◐ IKE quirks"]
  V4["Wi‑Fi guest + PCAP — ✓ offline"]

  RXF["EmbedXPL-Forge maps here as:<br/>target_device_class router/fw/ngfw • AutoPwn timing T0–T5 • CVE lookup after banner"]

  CORE --- COMBO
  COMBO --- V1
  COMBO --- V2
  COMBO --- V3
  COMBO --- V4
  V1 --- RXF
