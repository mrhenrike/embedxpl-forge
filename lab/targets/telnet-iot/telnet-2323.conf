service telnet-2323
{
    type         = UNLISTED
    port         = 2323
    socket_type  = stream
    protocol     = tcp
    wait         = no
    user         = root
    server       = /usr/sbin/in.telnetd
    server_args  = -L /bin/login
    disable      = no
}
